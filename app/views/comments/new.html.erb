<%= form_tag post_comments_path(commentable_type: 'post', commentable_id: @post_id), method: :post do %>
  <%= text_area_tag :comment_text, placeholder: 'Enter Comment' %>
  <%= submit_tag :Comment %>
<% end %>
